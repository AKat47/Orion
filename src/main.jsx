import React from "react";
import ReactDOM from "react-dom/client";
import OvionSlide from "./OvionSlide.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OvionSlide />
  </React.StrictMode>
);
